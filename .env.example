# ── Auth (physician sign-in — same API as tab app) ───────────────────────────
# Optional dev defaults (match tab appConfig):
# VITE_DEFAULT_PHYSICIAN_EMAIL=james@telecare.com
# VITE_DEFAULT_PHYSICIAN_PASSWORD=demo-password

# ── Data APIs (incidents, physicians, cases) ────────────────────────────────
# Live production APIs (same as tab app):
VITE_API_BASE_URL=https://api.tiatele.databin.in/api
# ECG: GET {VITE_API_BASE_URL}/ecg/:incidentId
VITE_AI_SUMMARY_URL=https://aisum.databin.in/case-summary

# ── Chat + calling (Chating-Backend — same as tab app) ────────────────────
VITE_CALLING_SERVICE_URL=https://tiacalling.tiamdplus.databin.in
# Local Chating-Backend: VITE_CALLING_SERVICE_URL=http://localhost:5200

